-cp src
-cp ../../bindings/haxe

# Set the LIBRL_ROOT environment variable to the path to the librl root directory.
# Note that this is relative to the output directory.
-D LIBRL_ROOT=../../../..

--main Main

-D analyzer-optimize
--dce std

-lib hxasync
--macro hxasync.AsyncMacro.makeAsyncable("")
--macro macros.ExportCABI.apply()
